This module exists to provide introspection for developers on site resources like
 * Models
 * Routes
 * Static files

Consider removing it from production deployments to reduce a little bit of overhead.